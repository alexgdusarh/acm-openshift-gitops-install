apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: placement-apps-prod
  namespace: openshift-gitops
spec:
  clusterSets:
    - prod
  predicates:
    - requiredClusterSelector:
        labelSelector:
          matchExpressions: []
