# NOT a new object -- this is the patch to merge into your existing hub `argocd`
# CR (namespace openshift-gitops) so its embedded Kustomize can run the
# PolicyGenerator plugin. Apply with:
#   oc patch argocd openshift-gitops -n openshift-gitops --type merge --patch-file argocd-enable-policygenerator.yaml
#
# UPGRADE CHECKLIST: whenever RHACM is upgraded on the hub, bump the image tag
# below (search: RHACM_VERSION) to match the new RHACM/multiclusterhub version,
# then re-apply this patch. Mismatched versions can ship a PolicyGenerator binary
# that doesn't understand newer PolicyGenerator fields.
spec:
  kustomizeBuildOptions: --enable-alpha-plugins
  repo:
    env:
      - name: KUSTOMIZE_PLUGIN_HOME
        value: /etc/kustomize/plugin
    initContainers:
      - name: policy-generator-install
        # RHACM_VERSION -- update this tag on every RHACM upgrade
        image: registry.redhat.io/rhacm2/multicluster-operators-subscription-rhel9:v2.15
        command: [/bin/bash]
        args:
          - -c
          - cp /policy-generator/PolicyGenerator-not-fips-compliant /policy-generator-tmp/PolicyGenerator
        volumeMounts:
          - name: policy-generator
            mountPath: /policy-generator-tmp
    volumeMounts:
      - name: policy-generator
        mountPath: /etc/kustomize/plugin/policy.open-cluster-management.io/v1/policygenerator
    volumes:
      - name: policy-generator
        emptyDir: {}
